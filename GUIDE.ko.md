# 처음 쓰는 사람을 위한 안내

프로그래밍이 처음이어도 따라 할 수 있게 쓴 글입니다. 순서대로 읽어 보세요.

## 1. 주사위 표기법이란

보드게임이나 TRPG에서 쓰는 약속된 표기입니다. `2d6+3` 하나면 "6면 주사위를 2개
굴려서 나온 값을 더하고, 거기에 3을 더한다"는 뜻이 전부 담깁니다.

| 표기      | 읽는 법                          | 나올 수 있는 값 |
| --------- | -------------------------------- | --------------- |
| `1d6`     | 6면 주사위 1개                   | 1 ~ 6           |
| `d20`     | 20면 주사위 1개 (`1`은 생략 가능) | 1 ~ 20          |
| `2d6`     | 6면 주사위 2개의 합              | 2 ~ 12          |
| `2d6+3`   | 위의 합에 3을 더함               | 5 ~ 15          |
| `4d8-1`   | 8면 주사위 4개의 합에서 1을 뺌   | 3 ~ 31          |

`d` 앞은 **주사위 개수**, 뒤는 **면의 수**, 그 뒤의 `+`/`-`는 **보정치**입니다.

## 2. 데모 페이지로 먼저 만져 보기

코드를 쓰기 전에 눈으로 확인하는 방법입니다. 터미널에서 이 폴더로 이동한 뒤:

```sh
npm install
npm run demo
```

브라우저에서 <http://localhost:3000/demo/> 를 열면 입력창이 나옵니다. `1d6`을
넣고 굴리기를 눌러 보세요. 버튼을 누를 때마다 값이 바뀝니다. `2d6+3`, `d20`처럼
바꿔 넣어도 되고, 일부러 `1d`처럼 잘못 넣으면 어떤 오류가 나는지도 볼 수
있습니다. 터미널에서 `Ctrl+C`를 누르면 종료됩니다.

## 3. 코드로 써 보기

### 설치

```sh
npm install @dgrr-studio/dice
```

### 가장 짧은 예제

`test.mjs` 라는 파일을 만들고 아래를 붙여 넣으세요.

```js
import { dice } from '@dgrr-studio/dice';

const d6 = dice('1d6'); // ① 주사위를 하나 선언한다
console.log(d6.roll()); // ② 굴린다
console.log(d6.roll()); // ③ 같은 주사위를 몇 번이든 다시 굴린다
```

터미널에서 `node test.mjs` 로 실행하면 숫자 두 개가 찍힙니다.

핵심은 **선언과 굴리기가 나뉘어 있다**는 점입니다. `dice('1d6')`은 주사위를
만들기만 하고 값을 정하지 않습니다. 실제로 값이 정해지는 건 `.roll()`을
부르는 순간이고, 그래서 한 번 만든 주사위를 계속 재사용할 수 있습니다.

### 주사위가 알려 주는 정보

```js
const attack = dice('2d6+3');

attack.roll(); // 11  (굴릴 때마다 달라짐)
attack.min; //  5  (가장 낮게 나올 수 있는 값)
attack.max; // 15  (가장 높게 나올 수 있는 값)
attack.count; //  2  (주사위 개수)
attack.sides; //  6  (면의 수)
attack.modifier; //  3  (보정치)
```

## 4. 자주 막히는 곳

**잘못된 표기를 넣으면 오류가 납니다.** 이건 버그가 아니라 의도된 동작입니다.
사용자가 입력한 값을 그대로 넘겨도 안전하도록, 이상한 값은 조용히 넘어가지 않고
바로 알려 줍니다.

```js
dice('1d6+'); // TypeError: Invalid dice notation: "1d6+"
dice('1d0'); // RangeError: A die needs at least 1 side: 1d0
```

사용자 입력을 받는 화면이라면 `try` / `catch` 로 감싸서 안내 문구를 띄우면
됩니다. 데모 페이지가 바로 그렇게 되어 있습니다.

```js
try {
  const d = dice(사용자입력);
  console.log(d.roll());
} catch (error) {
  console.log('표기법이 올바르지 않습니다:', error.message);
}
```

**여러 번 굴린 결과를 모으고 싶다면** 배열로 직접 모으면 됩니다.

```js
const d6 = dice('1d6');
const 열번 = Array.from({ length: 10 }, () => d6.roll());
```

**매번 같은 결과가 나오게 하고 싶다면** (예: 테스트를 쓸 때) 두 번째 인자로
난수 함수를 직접 넘기세요. `0` 이상 `1` 미만의 숫자를 돌려주는 함수면 됩니다.

```js
const 항상6 = dice('1d6', () => 0.999);
항상6.roll(); // 언제나 6
```

## 5. 용어 정리

- **패키지(package)**: 남이 만들어 둔 코드 묶음. `npm install`로 내 프로젝트에 가져다 씁니다.
- **선언(declare)**: 값을 바로 구하지 않고, 나중에 쓸 대상을 미리 만들어 두는 것.
- **인자(argument)**: 함수 괄호 안에 넣어 주는 값. `dice('1d6')`에서 `'1d6'`이 인자입니다.
- **난수(random number)**: 예측할 수 없이 뽑히는 수. 주사위의 무작위성이 여기서 나옵니다.

더 자세한 API 표는 [README.md](./README.md)에 있습니다.
